# Product

## Register

product

## Users

**待定。** 用户尚未确定这个应用面向谁 —— 这是有意留白，不是遗漏。

目前唯一确定的事实：应用处于骨架阶段，功能刻意留空，架构先于功能定型。
在用户明确目标人群之前，任何"目标用户画像"都是编造，设计决策不得以此为据。

已知的唯一使用场景：开发者本人在 macOS 上运行、迭代这个壳。

## Product Purpose

一个 macOS 原生应用骨架，架构、构建、测试全部接通，业务功能留空待填。

成功的定义（当前阶段）：新增一个功能模块时，开发者不需要重新做架构决策 ——
照着 `FeatureHome` 抄，编译器会把该接的线都提示出来。

## Brand Personality

**密集 · 专业 · 信息优先**

一屏尽量多放信息，不为美观牺牲密度。参照系是 Xcode、终端类工具，
不是留白充裕的消费级应用。控件紧凑，标签简短，图标优先于文字标签。

间距已按这条收过一轮：主要用 `Spacing.sm/md`，`xl/xxl` 只留给空态；
列表行两行文字约 42pt；路径一律只显示末两级，完整值进 tooltip。

## Anti-references

**SaaS 仪表盘那套。** 具体指：

- 大数字 + 小标签的指标卡片（hero-metric 模板）
- 渐变色块、渐变文字
- 千篇一律的「图标 + 标题 + 描述」卡片网格
- 用卡片包一切 —— 卡片是偷懒的答案，只在它确实是最佳载体时才用

## Design Principles

1. **只换绘制，不换行为。**
   界面要长得像被设计过，不是像一个默认的 AppKit 窗口 —— 所以表面、边栏、
   标题栏、按钮、列表行都是自己画的。但换皮的范围有一条硬线：
   **能只接管绘制的才换**（`ButtonStyle` 只管画，点击 / ⏎ / Tab 焦点 /
   禁用传播一点不丢），**要连交互一起重写的不换**（开关、下拉、滚动条、
   `TextField` 本体、设置窗口的分页）。
   自绘的每一处都要把原生白送的东西一件件补回来：悬停 / 按下 / 禁用三态、
   键盘可达、VoiceOver 的名称与 `.isSelected`、减弱动态降级。
   补不齐就别换。清单见 `DESIGN.md` 的「从系统默认到设计过」。

2. **密度是功能，不是妥协。**
   在信息量和呼吸感之间冲突时，倒向信息量。图标优先于文字标签（配 tooltip），
   紧凑控件优先于默认尺寸。

3. **自己的色阶，自己扛适配。**
   系统语义色白送三种外观的适配，但它给不了「外壳 / 内容 / 浮起 / 陷入」这四层
   稳定的关系 —— 那正是界面看着像被设计过的原因。所以换成自己的色板（三套，可切换）。
   代价必须一次付清：每个色写成 OKLCH 的**四组值**（浅 / 深 / 增强对比度浅 / 深），
   由 `dynamicProvider` 在绘制时挑；对比度**算过再提交**，正文四种外观全部 ≥ 4.5:1，
   增强对比度那两组 ≥ 7:1。做不到这两条，就退回语义色。
   品牌色只用在主操作、当前选中、状态指示上，不做装饰。

4. **动效只表达状态变化。**
   过渡时长 150–250ms。没有入场编排，没有滚动触发的揭示动画。
   如果一个动效不在告诉用户"某个状态变了"，它就不该存在。

5. **无障碍是默认路径，不是补丁。**
   减弱动态、增强对比度、完整键盘操作、VoiceOver 标签 —— 四项全要。
   原则 1 是达成这一条最省力的手段：原生控件默认就守这些。

## Accessibility & Inclusion

四项全要，无例外：

| 项 | 要求 |
|---|---|
| 减弱动态效果 | 系统开启时，所有过渡降级为淡入淡出或直接切换 |
| 增强对比度 | 系统开启时，分割线与次级文字加重（语义色自动处理） |
| 键盘操作 | 每个交互控件可 Tab 到达，焦点环可见 |
| VoiceOver | 每个控件有正确的辅助标签与角色；图标按钮必须带可读名称 |

正文对比度不低于 4.5:1，大号文字不低于 3:1。色阶是自己的，所以这条要自己算 ——
`Theme.Ink.secondary` 四种外观下都验过 ≥ 4.5:1，可以放正文；
`Theme.Ink.tertiary` 只够 3:1，**只能放大号或装饰性文字**。

「选中」和「键盘焦点」是两件事，允许同时出现在不同的项上：
强调色块表示当前分区，系统蓝焦点环表示键盘停在哪。这是 macOS 标准行为。
