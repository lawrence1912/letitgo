# Design System

letitgo 是个装工具的外壳，视觉上要做到两件互相拉扯的事：
每个工具有自己的性格，但整个应用像一个东西。

做法：**六套色板（棱镜 / 星云 / 霜白 / 莫兰迪 / 雾青 / 素白，设置里可切换，默认棱镜）；
主窗口以一颗全窗口能量星球为底，chrome 和控件是压在它上面的系统 Liquid Glass；
拿来读和改的东西不上玻璃。**

## Register

Product UI。设计服务于任务，不是设计本身就是产品。
参照系是 Xcode、Linear、Raycast 这类工具，不是营销落地页。

玻璃在这个 register 里是**分层手段**，不是主题皮肤：它负责说清「谁浮在谁上面」，
不负责好看。所以没有彩色玻璃卡片、没有到处飘的模糊圆球 ——
**卡片本身永远不上色**，颜色只出现在氛围场和那几个语义角色上。

它也不是**到处都铺**：玻璃给 chrome 和控件，拿来逐字读的东西（代码框、结果区）
走实色。判断标准是「这上面的字要读多久」，不是「这块大不大」。

饱和度是**每套主题自己的选择**：降饱和那几套把氛围底压到扫一眼说不出是什么颜色；
棱镜把它拉到能一眼认出「电光青」「电蓝」。这条线上两端都合法，因为不动的是别的东西 ——
分层靠材质本身的折射和边缘，不靠给卡片刷颜色；一屏仍然只有一个响亮的东西；
还能算的那几格对比度都是算出来的。

降饱和同理：它是**降噪**，不是复古滤镜。一屏工具界面里同时亮着六七个语义色，
高饱和会让它们互相打架，眼睛找不到该看哪儿。全部拉灰之后，
唯一还敢响的那个东西（主按钮）就自动成了视线落点。

降饱和那几套（莫兰迪 / 雾青 / 素白 / 霜白）都守这条 —— 换主题换的是**色相**，
不是响度。素白连色相都不换（它一点色都不上），换掉的是「强调色靠什么跳出来」：
不靠色相，靠明度的两端。所以它是「今天想看暖的、冷的，还是不带颜色的」，
不是「今天想不想被界面吵」。

**响度也是一个可以调的轴，只是它有闸。** 星云把它调了一档（琥珀、能量星球、
一层网格），棱镜调到头（电光青、高饱和能量环、最亮的氛围场）。
这两套没有推翻上面那条，是给那条加了个前提：

- **响的永远只有 accent 和四个语义色。** 正文、边框、图标底板在六套里是同一档
  中性 —— 棱镜的正文 chroma 压在 0.03 以内，底下越吵，字越要安静。
- **一屏仍然只有一个响亮的东西。** 强调色的用量规矩（主操作 / 当前选中 / 状态指示）
  一个字没改，鲜艳的是那一块的**颜色**，不是它的**数量**。
- **响多少是被对比度测试卡着的。** 星云和棱镜都是被逼着退回来的（各自那节记了过程）：
  测试不问「好不好看」，只问「这段字还读不读得清」。

---

## 从「系统默认」到「设计过」，再走回来一半

最早这里全走 `NSColor` 语义色 + 原生控件，省事，代价是**长得像一个没人设计过的
AppKit 窗口**。于是上一版把能自绘的都自绘了：标题栏、侧边栏、分段控件、
按钮、表面。

**macOS 26 之后，那批决定有一半的前提没了。** 当时自绘的理由几乎都是同一句话
——「系统那块是不透明的灰贴图，混在自己这套色阶里是异物」。现在系统那块是
Liquid Glass：它折射背后的内容、跟着指针形变、在减弱透明度时自己让位。
继续自绘等于**用一层贴在屏幕上的颜色去模仿一件系统真的会做的事**。

所以这一版是**往回还**，但不是全还：

| 上一版 | 这一版 | 为什么 |
|---|---|---|
| 自绘薄膜 + 边缘高光 + 投影 | `glassEffect` | 自己画的不折射，也不知道背后是什么 |
| 自绘按钮（`GlassChip` × 四种） | 系统材质 + 自己补高光 | 材质和交互反馈还给系统，只补它不给的那几层光 |
| 自绘分段控件（三处一套零件） | **仍然自绘**，但滑块是真玻璃 | 实测系统分段控件不是玻璃（见下） |
| 自绘导航项 + `matchedGeometryEffect` | `List(selection:)` | 上下键、首字母跳转、分组语义全白拿 |
| 隐藏标题栏 + 自绘 `PageHeader` | 原生标题栏 + `.toolbar` | 工具栏是玻璃了，还自带滚动边缘渐隐 |
| `safeAreaInset` + 自画模糊条 | `safeAreaBar` | macOS 26 专门给贴边 chrome 的 API |
| 六个槽位的实心色阶（减弱透明度的替身） | 删了 | 系统材质自己会让位，不需要我们备一套 |

**没还回去的两件事**（理由都在下面）：

| 仍然自己来 | 为什么 |
|---|---|
| 氛围场（`AmbientBackdrop` + 星球 + 网格） | 玻璃得有东西可折射，而那东西的亮度必须是已知的 |
| 边栏选中块的**颜色** | 它跟系统强调色走，改它要资源目录，而本机编不了（见下） |
| 玻璃上的**高光层**（斜光 / 镜面边 / 顶面亮带 / 辉光） | 系统材质是给 OS chrome 调的，克制是它的设计目标 |

一句话变了半句：**能还给系统的就还，还不回去的才自己画。**

---

## 玻璃

表面是**系统的一层光学材质**，不是自己拼出来的一叠颜色。
`Glass.swift` 因此从 741 行掉到 170 行 —— 删掉的是薄膜、边缘高光、投影、
控件上那道斜光，以及顶面高光 / 底缘聚光 / 色散边那一套「宝石」。

### 模仿再准也只是模仿

上一版那三件套（薄膜 + 边缘高光 + 投影）是照着玻璃的**结果**画的，
画得挺像。但它补的是「看上去像玻璃」，不是「是玻璃」：

一层自己画的颜色**不知道背后是什么**。所以它不折射，边缘的浓度不会随背后的
亮暗改变，挪动窗口时它纹丝不动 —— 而「挪一下就看出哪一层在动」恰恰是玻璃
唯一骗不了人的地方。

`glassEffect` 是后者：它**采样背后的内容**，把它弯折、聚焦、按亮度自适应前景。
同一块面板压在星球的亮环上和压在暗处是两种边缘，而这件事没有任何一组静态色值
写得出来。

### 一条硬规矩：玻璃底下必须有东西

反过来说，一块玻璃背后什么都没有的时候，它折射的是虚空 —— 出来就是一块灰板。

**所以氛围场不是装饰，是这套材质的前提。** 星球、外晕和那层细网格给玻璃提供了
可折射的起伏和可对齐的规律。

这条也是判断「拖出窗口的东西该不该上玻璃」的依据：拖动待办卡时跟着鼠标走的
那一小块是 AppKit 单独截图画的，**背后没有氛围场**，所以它走实色
（`Surface.well`），不上玻璃。

### 四档材质，以及一次选错了档

这一节推翻过三次。前两次是同一个错误的两面，第三次才是真正的病根。

- **第一版**只用 `glassEffect`：一屏卡片出来是**一排深色方块**。
- **第二版**往上堆高光（斜光 0.16、按钮上一道 0.98 的亮带）：**一排塑料糖豆**。
- **第三版**换成「烟熏面」（系统材质 + **0.78 的幕**）：看着体面了，但**背景全被吃掉**。

第三版最值得记，因为它看起来是对的。烟熏是个**真实存在的 profile**，
但它是给「dark utility 工具卡」用的 —— 那一档的目标是**压住背景、让卡沉下去**。
而我们参照的那张图不是那个 profile，它要的是**清透的暗玻璃**：
背后的光要透得出来。

**0.78 的幕不是「效果调弱了」，是选错了档。** 量出来的差距非常直白：

| 工作区里亮于 0.02 的像素 | |
|---|---|
| 参考图 hero | 21.5% |
| 烟熏卡（0.78 幕） | **3.0%** |
| 清透卡（0.12 压暗） | **21.0%** |

**一个参数从 0.78 改到 0.12，背景的可见光多了七倍。** 前两版在高光上反复调，
调的全是错的那个旋钮。

现在的四档：

| 档 | 是什么 | 用在哪 |
|---|---|---|
| 1. **窗口场** | 近黑的不透明底 | 整扇窗口的锚 |
| 2. **氛围** | 星球 + 光丝 + 细网格 | 场之上、工作区之下 |
| 3. **清透玻璃面** | `Glass.clear` + **0.12** 压暗 + 三层边 | 入口卡、输入框、结果区 |
| 4. **上了 tint 的玻璃** | `Glass.regular.tint(...)` + 同色渐变垫底 | 当前选中、主操作、分段滑块 |

**烟熏那一档还留在代码里**（`smokedCard`），但现在没人用 —— 它是另一个 profile
的工具，不是这个的。留着是因为下次真要做 dark utility 时不用重写。

### 边是三层，不是一圈

清透玻璃的边**不能是一圈均匀的亮线** —— 那是霓虹灯，不是玻璃。三层各司其职：

| 层 | 画什么 | 说的是 |
|---|---|---|
| **局部辉光** | 2px 描边模糊 5，**mask 到左上那一段** | 光只照到这几条棱 |
| **锐边** | 1px，两端亮（0.80 / 0.65）中间暗（0.10） | 棱的方向 |
| **内衬边** | 0.5px，往里缩 1.5pt | 这块玻璃有厚度 |

辉光里必须留得住那条锐边的硬芯 —— 只有光没有芯，读成的是一团污渍。

### 面上的光只有 0.045

**强光归边，面要暗。** 斜光从第二版的 0.16 一路收到 0.045 ——
一层看得见的白洗读成的是「这块东西是白的」，不是「有光扫过它」。

预算差一个数量级是有理由的：**1px 的边不进对比度合成链**（正文压不到边上），
所以它可以到 0.80；斜光就压在正文底下，进链。

### 分段控件：滑块是**同一块**玻璃挪过去

分段控件属于第 4 档（交互玻璃）。中间有一版换回了系统的
`Picker(.pickerStyle(.segmented))`，理由写的是「macOS 26 的分段控件本身就是
Liquid Glass 了」。**实测不是** —— 它仍然是一块相当平的控件贴图，滑块是实色，
混在这套界面里不是一家。所以它回到自绘。

值得记的是**滑块怎么动**。这里用了 `glassEffectID`，而这是它唯一站得住的用法：

> 选中块在几个位置之间**真的移动**，系统于是把它当成同一块材质做形变过渡 ——
> 玻璃会拉长、收拢、重新聚焦。

对照边栏：那里是**十一行各自一块底**，没有「同一块东西挪过去」这回事，
所以只能瞬时切换（淡入淡出一整层材质会花眼，见「选中态：试错了三轮」）。

**同一个问题在两种结构下有两个答案，区别在于「是不是同一块东西」。**
判断标准不是「哪个动效更好看」，是**哪个动效在描述一件真实发生的事**。

### 阅读区使用稳定底色

输入框、代码和结果区使用不透明的 `Theme.Surface.well`，隔开星球和光丝纹理。
它保留各主题的浅色、深色与增强对比度色阶。普通边界用细线，输入焦点用强调色实线；
清透玻璃继续用于导航和短控件。开启减弱透明度时，读写区不需要更换材质。

### 响度：tint 只说「多重要」

tint 在这套界面里只表达一件事：**这块东西此刻有多重要。**
它不是装饰色，也不是分类色（分类是图标色的事，见下面「唯一的例外」）。

| | tint | 边 | 谁 |
|---|---|---|---|
| **主操作** | `accentFill` 0.30 + 底下一道 0.46→0.20 的同色渐变 | 同色 0.76 | 一屏最多一个 |
| **当前选中** | 同上 | 同色 0.76 | 边栏 / 主题格 / 待办卡 |
| **大卡** | 无 | 自己的辨识色 0.22（悬停 0.38） | 概览的入口墙 |
| **别的** | 无 | 发丝线 | 绝大多数容器 |

**为什么 tint 底下还要垫一道渐变**：只给 tint 的话，玻璃压在深色氛围场上会把
颜色吃掉大半，那块青会淡到认不出来。垫了同色的一层，颜色才立得住 ——
这是「上了色的玻璃」和「一块半透明的颜色」之间的差别：前者的颜色**来自光**，
所以需要一个光源垫在底下。

主操作和当前选中用**同一套配方**，这是有意的：它们是全应用仅有的两处重 tint，
而且从不同屏出现在同一个位置。一屏最多一个响亮的东西 —— 这条没变，
变的是「响亮」现在由 tint 的重量表达，不是由一块亮色填充。

### `GlassEffectContainer` 会把该分开的东西粘在一起

它的用途是把几块玻璃**合并成一块**渲染 —— 让它们的边互相影响、
让选中块在其间形变。**这不是「优化」，是一个会改变外观的语义。**

用错的代价很具体，两处都是实测出来的：

| 用在哪 | 结果 |
|---|---|
| 概览的入口卡 | 相邻卡片的内容**渗进对方的玻璃里** —— 一张卡的左边缘浮着隔壁卡的半个字 |
| 分段控件（配 `glassEffectID`） | 滑块**盖住了自己那格的图标** —— 外观切换选中的那档只剩一块青色，月亮没了 |

判断标准：**这几块玻璃是不是「一块东西」？**
入口卡是各自独立的面板 → 不包。分段滑块是同一块底在挪 → 本该包，
但实测系统合并渲染时把它压到了内容上面，所以改用 `matchedGeometryEffect`
挪一块自绘的底。

### 代价：对比度不再全是「算出来的」

上一版有一整套合成测试，把「氛围底 → 每一层薄膜 → 文字」逐层算一遍。
那套算法的前提是**每一层都是我们自己画的**。现在这个前提没了：
系统材质怎么混，我们既不知道也不该假设。

继续拿一个编出来的 alpha 去「合成」它，算出来的数会很好看，但它是假的 ——
**而一个假的对比度测试比没有测试更坏。** 所以测试退回到还归自己管的两块底，
细节见下面「对比度是算过的」。

### 让位是系统的事了

减弱透明度、增强对比度这两档，材质自己会响应 —— 上一版那套「薄膜换实心替身、
模糊层不创建」因此整套删掉，连带六个只为它存在的色板槽位。

**还归我们管的是氛围场那一层**：增强对比度下光晕和网格全部归零（有测试盯着），
减弱透明度时整颗星球不创建。气氛不该挡路，这条没变。

---

### 氛围场：折射的是自己画的东西，不是桌面

macOS 上最抢眼的做法是让玻璃**透到桌面**。**这里没有这么做**，理由只有一个：
壁纸是用户的，亮度随时会变 —— 卷进来之后，这套界面里唯一还能算的那一层
也没了。

算过：浅色外观下把一张黑壁纸垫在半透明表面后面，次级文字从 6.6:1 掉到 2.7:1。

所以底下垫的是应用自己画的 `AmbientBackdrop`：不透明基底上放
**一颗由外晕、暗球体、环外同心波纹、外围星光粒子和能量环组成的星球**，
再叠每套主题自己的可选 40pt 细网格。亮度带已知，所以它是这套界面里唯一
还能把对比度算准的一层 —— 代价是看不见壁纸。

它同时也是**玻璃的燃料**：上面那条硬规矩说的就是这一层。

星球是**主窗口背景，不是概览页的 hero 卡片**。它不属于任何工具页面，
页面切换和内容滚动都不会带着它走，也不占一行布局高度。
它固定在整扇窗口的同一坐标里，前景面板自然从它上面经过。

构图跟窗口短边缩放：直径为短边的 62%，并夹在 260–520pt；
圆心放在窗口宽度 66%、高度 52% 处，避开侧栏和正文的起始线。
窄窗不会被星球铺满，超宽窗也不会把能量环无限放大。

`NavigationSplitView` 会把分栏桥接成不同的 AppKit hosting 子树，
所以具名坐标空间不能可靠地从列内背景回溯到根。
`ambientField()` 在根部读取全窗口的 global `CGRect` 并通过 Environment 下发；
每个区域再用自己的 global frame 算出相对偏移，画同一张背景的对应裁片，
最后 `.clipped()`。侧栏、详情和根兜底因此没有接缝，也不会各画一颗星球。

#### 星球是个光源，不是一张贴图

第一版把它画成了「几圈线 + 一团雾」：能量环从头到尾一样亮，球体中心比周围亮一点，
外晕淡得几乎不存在。出图之后它读成的是**一组同心圆线框**——
而一组同心圆压在工作区底下，除了让人以为界面画错了之外没有任何作用。

问题不在浓度，在**光从哪儿来**：环上每一处一样亮，就等于说这里没有光源。
现在整颗星球服从一个方向 —— 光从**左上**打过来，和系统玻璃那圈边缘高光
落的是同一个角：

| 层 | 画什么 | 说的是 |
|---|---|---|
| 外晕 `deep` | 一团宽而软的光，峰值压在**环所在的半径**上 | 这团东西在往外漏光 |
| 波纹 `trail` + `lead` | 环外三道细同心线，最亮的一段永远在左上 | 能量在往外扩散，不是三圈边框 |
| 球体 `trail` | 圆心垫氛围底，只在贴近边缘处给到 0.62 | 它是个**实心的暗球**，不是一块色斑 |
| 边缘光 `trail` | 球体左上那道内弧 | 球有体积（圆没有厚度，球有） |
| 能量环 `lead` + `deep` | 左上一道**热弧**，绕回右下衰减到微光 | 光源在左上 |

能量环是两层：**细而实的核 + 宽而糊的雾**。两层用的是**不同的槽位** ——
核是 `auraLead`（近白，满强度），雾是 `auraDeep`。这不是配色偏好：
同一档色叠两层的话两层得分着用强度，核最多只能拿到七成，
出图是一圈发灰的胖环。**一条糊的亮线读成的是雾，不是光** ——
光要有个看得清的核，雾负责说明这个核在发光。换两个槽位之后核可以拿满，
而「lead 叠在 deep 上」本来就是亮度带里已经建模的一格，一格都不用新开。

**亮度是有闸的**，而且闸在测试里：星球压在整扇窗口底下，前景每一块半透明面板
都要从它上面经过。所以守一条硬规矩 —— **同一档 aura 色在任何一个像素上的
实际叠加，不超过它 token 自己的 alpha**（`Composite` 的亮度带正是按这条建模的）。
想让星球更亮，改**色板里的槽位**（那样对比度测试会跟着算），
不要在 `AuroraOrb` 里多叠一层。

某个槽位给全透明时，它负责的层就消失；`auraDeep` 透明的安静主题因此不画外晕与
粒子外晕，粒子核心、球体、波纹与能量环仍在。色相规律没有变：素白只做明暗，
星云沿琥珀 → 珊瑚 → 品红的暖色段，棱镜沿青 → 电蓝 → 靛紫的冷色段。

可用性仍然优先于特效：亮的部分全部收在环、边缘光和波纹**那几条细线**上
（细线不挡字），球体那一大片是暗的 —— 它是唯一会被大面积正文压住的一层。
星光只用低透明 aura 色，不用近白；浅色下线更粗、粒子更大，深色下稍细；
粒子和波纹都保持静止；开启减弱透明度时整颗星球不创建，增强对比度下
aura 与网格全部归零；设置窗口只留纯净基底。

---

## 颜色

色值全部写成 OKLCH，四种外观（浅 / 深 / 增强对比度浅 / 增强对比度深）各一组，
由 `NSColor(name:dynamicProvider:)` 在绘制时挑。视图层只写 `Theme.Ink.primary`，
用户切外观时它自己变。

用 OKLCH 不用十六进制：同一个 L 在不同色相下看起来一样亮，调色阶只改一个数。
转换在 `OKLCH.srgb`，30 行，不依赖任何库。

### 槽位 / 色板 / 主题

三层，别混：

| | 是什么 | 在哪 |
|---|---|---|
| **槽位**（`ColorToken`） | 「界面里的这个位置该有个颜色」，26 个 | `Palette.swift` |
| **色板**（`Palette`） | 一套主题给每个槽位填的 `Ramp`（四种外观各一组值） | `Palettes/*.swift` |
| **主题**（`AppTheme`） | 用户在设置里选的那个名字 | `AppCore/AppTheme.swift` |

`Palette` 里的 switch 是**穷尽**的 —— 加主题时漏填一个槽位编译不过，
不会出现「换了主题某处还是上一套颜色」。

**视图层永远不写 `ColorToken`。** 它写 `Theme.Ink.primary` / `Theme.Line.hairline`，
那些名字底下才是槽位。所以加一套主题不需要碰任何一个视图文件。

### 换主题保留编辑状态

当前主题仍存于 UserDefaults，由应用入口同步到可观察的 `PaletteStore`。
颜色令牌使用计算属性：视图取色时观察主题，`NSColor` 再根据系统外观解析色值。
切换主题只触发颜色消费者重绘，不再用 `.id(theme)` 重建页面；输入、选项、焦点和撤销记录保留。
Canvas 在视图求值时捕获颜色，避免绘制闭包绕过主题观察。

### 六套主题

莫兰迪和雾青是**降饱和**那一档：chroma 一律压在 0.06 上下（`danger` 例外），
靠色相区分角色，不靠饱和度。素白把这条推到尽头 —— chroma 直接是 0。
霜白走的是另一个方向：**把底抬到近白**（L 0.968，中性 chroma 0.007）。
星云走到另一端：**深空底 + 带粒子与波纹的能量星球 + 细网格 + 尾光**，琥珀点睛
（2026-09-03 从冷调换成暖调，见下）。
棱镜站在这条线的另一端：**饱和度拉满**，高饱和的星球把氛围场推到能一眼认出
「电光青」「电蓝」。

六套换来换去，层次关系、一屏只有一个响亮按钮那条规矩全部原样成立，
变的只是这一屏偏暖、偏冷、更亮、更深、更艳，还是干脆不带颜色。

> **下面这几节记的是每套主题「为什么是这个色相、这个亮度」，那部分仍然成立。**
> 但其中提到具体玻璃槽位（薄膜浓度、辉光、色散边、体内沉色、斜光、`onAccent`）
> 的段落记的是**上一版自绘玻璃时的调校过程** —— 那些槽位已经随
> `Glass.swift` 一起删了（见上面「玻璃」一节）。留着是因为它们记的是
> **怎么权衡**，那个思路比那几个数字活得久。

| 角色 | 棱镜（默认） | 星云 | 霜白 | 莫兰迪 | 雾青 | 素白 |
|---|---|---|---|---|---|---|
| 中性 | 蓝 255，深底 L 0.172 | 暖炭 45，深空 L 0.195 | 冷蓝灰 228，近白 | 暖灰 85（石灰 / 亚麻 / 旧纸） | 蓝灰 235 | 中性灰，chroma = 0 |
| `accent` 主操作 / 选中 | 电光青 202 | 琥珀 78 | 湖蓝 212 | 陶土 62 | 青 196 | 石墨（色阶的两端） |
| `info` 信息 / 拖放 | 蓝 265 | 紫 300 | 蓝 255 | 雾霾蓝 238 | 蓝 258 | 蓝 250 |
| `success` 成功 | 绿 145 | 绿 145 | 绿 148 | 鼠尾草 150 | 草绿 138 | 绿 148 |
| `danger` 失败 / 破坏性 | 红 20（chroma 0.135） | 红 25（亮一档） | 红 25 | 干玫瑰 25 | 红 25 | 红 25 |
| 星球 aura（环 / 球体 / 外晕 / 波纹 / 星光） | 近白青 205 + 电蓝 258 + 靛紫 282 | 琥珀 60 + 珊瑚 20 + 品红 330 | 湖蓝 200 + 蓝 250 | 干玫瑰 38 + 鼠尾草 158 | 蓝 252 + 青 196 | 一提亮 + 一压暗 |
| 网格 | 无 | 有（40pt，5%）/ 玫瑰 350 | 无 | 无 | 无 | 无 |

除素白外的光晕都是**邻近色**，不是补色：莫兰迪的张力来自挨得很近的几个脏色互相压着 ——
雾青沿用同一条规矩，只是把那几个脏色换到了蓝青那一侧；霜白的两团淡到 0.22 的透明度，
近白的底上再重一点就成了「渐变背景」。

霜白的 `accent` 一开始放在 188，出图一看就退了回来：待办板上「这张卡被选中」（accent）
和「这件事做完了」（success 148）会读成同一种青。**低饱和 + 近白的底上，
角色之间只剩色相一个维度可用**，所以 accent 挪到 212，和绿拉开 64 度。

`danger` 的 chroma 在降饱和那几套里是**同一个值**（0.105，别的角色一半不到；
棱镜抬到 0.135，因为它周围全是饱和色）：
**一个被调进壁纸里的警报不是警报。** 雾青里它还额外承担一件事 ——
其余三个角色都在冷色那半边（258 / 196 / 138），红是唯一跳出来的颜色；
霜白（255 / 212 / 148）同理；星云换暖之后 accent 和 danger 只隔 53 度
（琥珀 78 / 红 25），靠的是「琥珀是填充、红是文字」这条分工 ——
两者从来不在同一个角色上出现；
素白里更彻底：它是整屏唯一一处饱和色。

只有星云把深色那档的**亮度**从 0.790 提到了 0.850 —— 不是想让它更响，
是它的面板底下压着光晕，比别的主题亮一档，红字不跟着亮就掉到 4.2:1。

雾青的 `success` 刻意压到 138 而不是跟着莫兰迪走 150：离青（196）太近会和
`accent` 糊掉。低饱和色系里，**角色之间的距离只剩色相一个维度**，得留够。

#### 星云：科幻风怎么不砸掉可读性

「毛玻璃 + 科幻 + 渐变」这套东西的默认结局是：底下一片发光的星云，
上面一层压不住的玻璃，正文糊在里面。这套主题第一版就是这么写的，
然后对比度测试一次报了 **16 条**，全部集中在一处：`dark` 外观的**面板**上。

三处改动把它救回来，顺序是「先动底，再动膜，最后才动字」：

1. **光晕压暗**：深色那档三团的 alpha 从 0.20 / 0.20 / 0.13 降到 0.14 / 0.13 / 0.10，
   峰值亮度也各降一档。渐变还在，只是不再往面板底下塞光。
2. **薄膜变薄**（和直觉相反）：面板从 0.090 降到 0.050。
   薄膜是**白**的 —— 底下已经有光了，再刷一层白，面板就亮到压不住字。
   这套主题的面板改成靠**边缘**立住：带色的描边 + 右下角那道尾光。
3. **字跟着亮一档**：次级 0.785 → 0.850，三级 0.680 → 0.770，四个品牌色同理。
   底亮一档，压在上面的东西就得跟着亮 —— 这不是「科幻风要发光」，是算出来的。
   （后来六套主题的二三档又整体提过一次，次级现在是 0.900 —— 见下面
   「对比度是算过的」。）

底色本身也来回了两趟：0.145 → 0.115（太黑，整块工作区看着像没渲染完，
玻璃压在纯黑上等于没有玻璃）→ **0.195**（深板岩，光晕铺得开，
面板和槽之间也重新分得出层次）。**深空不等于纯黑** ——
黑到没有细节可折射的时候，这套东西就只剩「暗」，没有「深」。

还有一处是出图之后才发现的：**槽（输入框）不能是灰的。**
第一版照别的主题给了一块中性灰（L 0.48 / chroma 0.016），压在这套的彩色底上
就是一条脏兮兮的灰带 —— 周围每一层都有色相，只有输入框没有，
它读起来不像「陷下去的一块玻璃」，像「贴上去的一块塑料」。
改成跟着氛围底走、和底同色温的一块（现在是暖炭 40），深色那档压到近黑 + 19.5%
（第一版 34% 太黑，见「输入框是凹的」那节）。

留下来的一条经验：**渐变越厚，能压在它上面的字就越少。**
所以星云把所有霓虹都发给了「会动的东西」（accent / 语义色 / 落点高亮），
正文、边框、图标底板一律中性 —— 一屏全是发光的字等于一屏都在喊。

#### 星云换暖：换色温要重算什么

2026-09-03 这套从冷调换成暖调：底 蓝紫 275 → 暖炭 45，`accent` 霓虹青 200 →
琥珀 78，三团光晕 青→紫→品红 换成 **琥珀 60 → 珊瑚 20 → 品红 330**，
尾光 紫 300 → 玫瑰 350。猎户座那一类星云本来就是暖的，所以结构一条没动：
三团光晕、那层网格、右下的尾光、「霓虹只发给会动的东西」，全部照旧。

**换色相不是把色轮转一下就完事**，有两件事必须重算：

1. **同一个 L 在两种色温下不是同一件事。** WCAG 亮度里绿通道占 0.72、蓝只占 0.07，
   所以同一个 OKLCH 明度下，暖色（红 + 绿）的实际亮度明显高于冷色（绿 + 蓝）。
   照搬冷调那版的光晕 L，深色外观的表面被顶亮了一档 —— 珊瑚色的分区图标压在
   悬停态上只剩 **4.30:1**，测试当场就红了。修法是浅色那档的三团整体抬亮 0.03
   （往底色靠，压得更浅），深色那档压暗 0.06；alpha 一个没动。
2. **暖色的色域比冷色窄得多。** L 0.87 的琥珀在 sRGB 里最多只能到 chroma 0.118，
   同一亮度的青能到 0.15 以上，而且越亮越窄。顶着上限写的话
   `OKLCH.srgb` 会把溢出的通道夹掉 —— **不报错，只是看着有点脏**，
   而且和你写在文件里的数不是同一个颜色。这套里每个带 chroma 的暖色值
   都贴着色域边留了一档余量（有一份色域检查脚本，跑一遍就知道谁越界了）。

主按钮那道渐变跟着改成**落日**：琥珀 78 → 橙 38，等明度，两端亮度比 1.06。
**停在 38 度不再往下走** —— 再暖 10 度就进了 `danger` 的色相区，
一个右下角泛红的主按钮读起来像是要删东西。

三个语义色一个没动（紫 300 / 绿 145 / 红 25）：换色温换的是底和 accent，
「成功是绿的、出错是红的」不该跟着主题走。紫在暖底上反而更立得住 ——
它现在是全屏唯一的冷色。

#### 棱镜：科幻蓝，以及「哪个色相在哪个亮度上最浓」

这套是「充满活力的现代色彩 + 玻璃形态」那条路走到头的样子，走的是最主流的
那套**科幻蓝**：近黑深蓝打底（255，深色 L 0.172），主操作是电光青，
三团高饱和光晕（青 195 → 电蓝 262 → 靛紫 285，冷色那半边的一整段光谱）
透过全应用最薄的薄膜（浅色 content 0.47，别的主题 0.50–0.52），
边缘是一圈泛青的发丝线。**没有为它新开任何画法** ——
薄膜 / 边缘 / 投影还是那三层，只是三个数都推到了另一端。

**主按钮不能是深色的。** 鲜艳和 7:1 直接打架：7:1 要求填充要么很亮（配近黑字）
要么很深（配近白字），而「鲜艳」的直觉答案恰恰是中间调。更早的一版走深色
（深紫配白字，裸算 7.9:1），结果它和这套自己的**斜光**打起来了：26% 的白洗上去，
白字掉到 6.7:1，再压深也救不回来。**斜光是「光」，光该落在亮的填充上** ——
深底按钮顶一道白光本来就自相矛盾。

**于是「哪个色相在哪个亮度上最浓」成了选色的第一条件**，而不是先挑色相再配明度。
sRGB 里这件事各色相很不一样：

| 色相 | L 0.62 | L 0.80 | L 0.86 |
|---|---|---|---|
| 青 202 | 0.106 | **0.137** | **0.147** |
| 蓝 250 | **0.175** | 0.105 | 0.072 |
| 蓝紫 265 | **0.204** | 0.100 | 0.068 |

**青是少数几个「越亮越浓」的色相之一，蓝正好相反。** 所以「亮填充 + 近黑字」
这条对青特别友好 —— 换成蓝就得在饱和度上大让一步（蓝橙那一版试过，
蓝端在 L 0.78 上只剩 chroma 0.114，出图看着是一块暖灰）。
这也定了那道渐变的跨度：青 202 → 电蓝 245，**只跨 43 度**。再往蓝里走十几度，
那一端就得压暗才保得住浓度，两端亮度差就过线了（测试卡在 1.25）。
**宁可跨度短一点、两端都够浓 —— 一道「浓 → 灰」的渐变看着不是渐变，是没调匀。**

同一条规律也决定了三团光晕各自的明度：左上那团青在深色外观下是**亮的**
（L 0.660），因为青在暗处几乎没有饱和度可言；而电蓝和靛紫那两团放在 L 0.47–0.50，
那是它们色域最宽的地方。压在近黑的底上，出来的正是 HUD 那种「屏幕自己在发光」。

浅色外观下三团只能是粉彩档（L 0.85 的蓝最多 chroma 0.072），
所以同一套色在浅色下像被阳光照透的玻璃、深色下才是 HUD ——
**这不是两种设计，是同一套色在两种亮度下各自能到的极限。**

没动的：正文 chroma 压在 0.03 以内（底下越吵，字越要安静）、
`danger` 抬到 0.135 且色相挪到 20（周围全是冷色，红是唯一跳出这段光谱的颜色）、
卡片 / 行 / 面板一律不上色、一屏仍然只有一个响亮的东西。

**后来往回收了一档：从「科幻」调到「现代」。** 上面那套是把响度推到头的样子，
问题不在对比度（测试一直是绿的），在于**它一眼就读成「特效」而不是「工作区」**——
深色外观下电蓝和靛紫顶着 chroma 0.215，那是 sRGB 在那个亮度上的色域边缘，
一块底铺满整扇窗、还顶着色域上限，屏幕看久了是累的。

于是两件事各让一步：三团的 chroma 收到 0.086 / 0.150 / 0.140（浅色档同步收到
0.078 / 0.050 / 0.044，alpha 各降两个点），薄膜整体加厚一档
（浅色 content 0.44 → 0.47，chrome 0.26 → 0.29，panel 0.42 → 0.45）。
**色相一个没换** —— 青 195 → 电蓝 262 → 靛紫 285 那段冷色光谱还在，
所以它仍然是「科幻蓝」，只是不再是霓虹；薄膜厚了一档之后它**仍然是全应用最薄的**
（0.47 < 霜白 0.50），玻璃形态没让步。

这一档的道理：**光晕是深度，深度不需要顶着色域上限。** 顶上去之后多出来的不是
纵深，是噪音 —— 而纵深那部分，加厚薄膜（把底压得更匀）比拉高饱和度更管用。

##### 能量环那一笔：拿透明度换明度

星球改成光源之后（见「星球是个光源」那节），环第一版仍然是 L 0.745 / alpha 0.175
的中明度蓝，出图读成一条灰线。直觉的修法是把 alpha 拉上去，**而那条路是封死的**：
星球压在整扇窗口底下，亮度带一抬，压在面板上的正文当场掉线 ——
试过 alpha 0.34，`inkTertiary` 从 4.65 掉到 2.38，一次报 30 条。

扫了一遍参数空间之后的结论很干脆：**深色档的三团 aura 早就贴在上限上了，
一档都抬不动。** 于是换了笔交易 —— 同样的亮度预算，花在**明度**上而不是浓度上：
L 0.900 / chroma 0.095 / alpha 0.135（色相跟着 accent 从 235 挪到 205）。
合成出来的亮度比第一版还低一点点（正文最差格 5.17 → 5.15，测试全绿），
但那条线本身是**近白**的。

**近白正是发光体该有的样子** —— 光源的核心总是往白里跑，颜色留给它周围那圈
光晕（`auraDeep`，现在担着这套的靛紫）去承担。一颗只有白线没有色的星球是张线框图，
一颗只有色没有白核的星球是团雾；两个槽位各管一头，才是「在发光」。

同一轮里还免费拿到一档存在感：**边加重了**（`glassRim` 深色 0.30 → 0.42，
`glassHighlight` 0.42 → 0.58）。边是一条 1px 的线，**不进合成链** ——
对比度算的是「文字压在表面上」，表面是薄膜叠出来的，边不参与。
所以把它加重不用还任何东西，而这套的薄膜已经薄到全应用最薄，
一块面板还立不立得住，本来就全看这条线。
（跟着要还一笔：输入框底边那道回光的系数得从 0.45 收到 0.28，
否则加重之后它会亮成一条横贯全宽的**下划线**——
一条下划线读成的是「这里有条分割线」，不是「这个槽是凹的」。）

#### 素白：没有色相的时候，强调色怎么办

一个中间调的灰按钮读出来是「这个按钮是灰的（禁用了）」。所以素白的 `accent`
不待在中间调，它走到色阶的尽头：浅色外观下近黑（比正文还深一档），
深色外观下近白（比正文还亮一档）—— 六套里只有它的
主按钮文字在浅色下是白的。

「选中」于是换了个说法：**往强调色那一端多走一步**，悬停只是浮起来一点。
别的主题靠「一个偏色、一个不偏色」区分这两个状态，素白只剩明度这一条线索，
所以 `accentSoft` 压得比另外两套重：浅色下选中块合成出来是 #d7d7d7、悬停 #e5e5e5，
深色下选中块 #434343、悬停 #323232。

三个语义色反而可以再低一档（`info` 0.045 / `success` 0.052，比雾青还低）：
周围没有别的颜色跟它们抢，中性底上的低饱和色不会被邻近的色相拉走。

### 用量

Restrained：品牌色只出现在**主操作、当前选中、状态指示**上。
不做装饰，不做分区底色，不做「让它好看点」的填充。

降饱和之后这条更好守了：`accentFill`（主按钮）是全屏唯一允许比周围响一档的东西。
**通栏的东西尤其要压** —— `NoticeView` 是整宽的，同样的 chroma 铺满一条会读成
「一块横幅」，所以两套主题里 `infoSoft` 都比别的 soft 再低一档。

一屏最多一个响亮的按钮。所以标题栏的分区动作是**次要样式**，
空态里那个大按钮才是主操作 —— 两个一样响的等于没有主次。

设置里那个主题选择器的选中态用 `accentSoft` 而不是 `accentFill`：
「选了哪套主题」是**当前选中**，和侧边栏的分区选中同级，不是主操作。

### 唯一的例外：分区图标是彩色的

上面那条「不做装饰」有一个例外，而且是**有条件的**例外。

条件是：颜色在那儿承担的是**辨识**，不是装饰。一个装了八个分区的壳，
图标全是同一档灰的时候，找「时间戳」只能靠读字；每个分区有固定颜色之后，
眼睛可以先按颜色缩小范围再读字。macOS 系统设置就是这么做的 ——
那一列彩色图标不是好看，是让你在二十个面板里一眼找到「网络」。

这个例外守着四条，缺一条它就退回装饰：

1. **色族写死**（`SidebarItem.tint`），一个分区永远是那个颜色。
   按下标算的话，中间插一个分区后面全跟着挪一位 ——
   「紫色那个是编解码」这条肌肉记忆就建立不起来，颜色也就白给了。
2. **和语义色分开**。`IconTint` 是另一套取色，`Tone`（accent / info /
   success / danger）一个没碰 —— 一枚红色的图标底板不该让人以为那个分区出错了。
3. **饱和度跟着主题走**。十个色族只提供**色相角度**，亮度和饱和度是所有主题
   共用的一套（和品牌色对齐）。所以莫兰迪里它们是脏的，雾青里它们是冷的，
   不会出现一套灰调配色里插着一块荧光蓝。素白里它们是全屏最鲜的东西 ——
   这是有意的：壳越安静，靠颜色找分区越管用，角度也就按色环摊平了排。
4. **留白分区留在中性档**。于是颜色顺带说明了「这个分区还没接」——
   概览的入口页本来就不列它们。

对比度照测：10 个色族 × 3 套主题 × 4 种外观，压在所有表面上 ≥ 4.5:1，
压在自己那块底板上也 ≥ 4.5:1，增强对比度那两档 ≥ 7:1。

### 对比度是算过的 —— 但这一版能算的少了

**先说少了什么。** 上一版把「氛围底 → 每一层薄膜 → 文字」逐层跑一遍，
对每个「文字 × 表面」组合取最差的一格。那套算法的前提是**每一层都是我们画的**。

换成系统玻璃之后这个前提没了。继续拿一个编出来的 alpha 去「合成」系统材质，
算出来的数会很好看，但它是假的 —— **而一个假的对比度测试比没有测试更坏**：
它会让人以为守住了，然后放心去调色值。

所以测试退回到**还归自己画**的两块底：

| 底 | 性质 | 为什么可信 |
|---|---|---|
| **氛围场** | 不透明，亮度带已知 | 玻璃压在它上面，所以它是**下界** |
| **实色兜底面**（`well`） | 不透明，单一色值 | **精确**。拖拽预览那种背后没有氛围场的东西 |

**自己加的那两层必须进链。** 系统材质算不了，不代表补上去的也可以不算 ——
斜光就压在正文底下，所以亮度带里包含「压了斜光」的那几格，
而且按它**峰值压满整块底**来算，比实际狠一档（实际是从左上化到透明）。
镜面边不进链：1px 的边正文压不到。

「氛围场是下界」是一条**假设**，不是算出来的结论：系统玻璃会把背后往中间调拉，
不会把对比度拉得比裸底更差。它写在 `Composite.Surface` 的注释里，
是为了让它可被反驳 —— 哪天发现某个外观下不成立，改的是那条注释和门槛，
不是悄悄调色值。

而且这个下界**不是纸上谈兵**：概览的分组标题、空态文案、状态栏那行字
本来就直接压在氛围场上，没有任何玻璃。

底线和实测（六套主题 × 四种外观取最差）：

| | 底线 | 实际最低 |
|---|---|---|
| `Ink.primary` / `Ink.secondary` / 四个品牌色 | 4.5:1 | 4.6:1 |
| `Ink.tertiary`（placeholder / 单位 / ⌘ 编号） | 4.5:1 | 4.5:1 |
| 徽章 / 提示条上的色调文字 | 4.5:1 | 4.5:1 |
| 彩色图标（压在两块底上） | 4.5:1 | 4.6:1 |
| 彩色图标压在自己那块底板上 | 4.5:1 | 4.7:1 |
| **选中块上压的正文**（边栏 / 主题格 / 待办卡） | 4.5:1 | 4.9:1 |
| 焦点环压在输入框自己那块底上 | 3:1 | 3.2:1 |
| 增强对比度那两档的正文 | 7:1 | 8.1:1 |
| 增强对比度那两档的 `Ink.tertiary` | 6:1 | 7.2:1 |
| 增强对比度那两档的彩色图标 | 7:1 | 7.9:1 |

两条测的不是对比度，是**不让约束烂在注释里**：

- **`well` 的 alpha 必须是 1。** 「被拎出窗口的东西要有一块确定的底」是设计约束，
  写在注释里会烂掉；哪天有人给它配了个半透明值，拖拽预览会露底，这条先炸。
- **增强对比度下光晕和网格全部归零。** 气氛不该挡路。

**改这一版时它抓到了三处**，全是原先被薄膜「垫」出来的假余量 ——
薄膜一撤，字直接落在氛围场上就不够了：莫兰迪的三级文字 4.43、雾青 4.50、
莫兰迪的青色图标压在自己底板上 4.49。改的是色值，不是底线。

**删掉的那几条**（主按钮 `onAccent` 的 7:1、强调色渐变两端等明度、斜光压满、
辉光的色相和 alpha）不是放宽了，是**那些东西不存在了**。

主按钮那条删得最晚，也最值得说：它一度是「自己画的一块亮色填充 + 顶面亮带」，
所以字色能算。现在那块底是**上了 tint 的玻璃**，系统怎么混我们不知道 ——
于是它和选中块、输入框归成同一类：**算不了的那一档，靠看**。

算不了不等于不管。这一档的验证方式是**从截图上量真实像素** ——
跑起来、截图、按 WCAG 公式算底色和笔画的分位数，而不是在色板里编一个 alpha
假装算得出来。

量出来的（棱镜，2026-09）：

| | 深色 | 浅色 |
|---|---|---|
| 主按钮的字 | **7.11:1** | 13.03:1 |
| 次要按钮的字 | 16.37:1 | — |

主按钮那个 7.11 是**调出来的**，第一版量到 4.69。修法不是把字调白
（它已经是近白了），是**在 tint 底下垫一层压沉的幕**：

> 4.69 那一版真正的毛病不是「不够」，是**「不稳」** —— 按钮底下就是那颗星球，
> 它压在亮环上和压在暗处是两个亮度，对比度跟着背景漂。
> 垫了那层幕之后这块底基本不受背后影响，才谈得上「量出来是多少」。

这条对整个第 4 档都成立：**上了重 tint 的玻璃要想守得住对比度，
底下就得有一层不透明的东西兜着。**

顶上来的是「选中块上压的正文」那一条，而它测的是真正会出问题的事：
选中块上压的不是强调色的字，是**正文**（分区名、待办的内容）—— 浅底一浓那行字就糊了。

**降饱和不影响对比度** —— 对比度只吃亮度（L），不吃饱和度（C）。
所以换色系时四个角色的 L 一个没动，动的全是 C 和色相。

---

## 排版

系统字体一个家族，靠字重和字号拉层次。产品 UI 不需要标题体 / 正文体配对。

```
display  19 semibold  字距 -0.3   页面标题
title    15 semibold  字距 -0.2   区块 / 空态标题
headline 13 semibold              提示条标题、行主文案
body     13                       正文
label    12 medium                按钮、徽章、导航项
caption  11                       次级说明
numeric  12 medium 等宽数字        计数、大小、时间
mono     11 等宽                   路径、端口、标识符
```

比例约 1.15，比 SwiftUI 默认语义字号密。标题带负字距 —— 系统字体在 15pt 以上
默认字距偏松。字号写死不做流体缩放：桌面 DPI 固定，缩放只会更难读。

---

## 密度

冲突时倒向信息量。主要用 `Spacing.sm(8)` / `md(12)`，`xl/xxl` 只用于空态。
列表行两行文字（主文案 + 元信息），高度约 42pt。

路径一律**只显示末两级**，完整路径进 tooltip —— 一整条绝对路径能把文件名和
大小全挤没，而用户在那一行要确认的只是「这东西从哪儿来的」。

---

## 组件

| 组件 | 用途 | 不要拿它做什么 |
|---|---|---|
| `EmptyStateView` | 「这里本来就没东西」，顺带教会用户这个界面干什么 | 报错 |
| `NoticeView` | 「有件事你必须知道」，**正文不截断** | 常驻的次要信息 |
| `Badge` | 离散状态。图标 + 文字 + 颜色三重编码 | 放一整句话（放不下就上 `NoticeView`） |
| `IconTile` | 行 / 空态的前导图标底板。两个入口：`tone:` 是**语义**（成功 / 失败），`tint:` 是**辨识**（哪一个分区） | 当按钮；拿 `tone:` 去做辨识色 |
| `NavigationCard` | 「点进去」的入口卡片。整张卡是按钮，卡面只有图标 + 名字 + ⌘ 编号 | 当展示用的信息卡（它是可点的）；在卡面上写说明 |
| `StatusDot` | 和文字一起出现的状态点 | 单独承担信息 |
| `AppearancePicker` | 外观切换（浅 / 深）。和上面共用同一套零件 | — |
| `SegmentedPicker` / `SegmentTrack` / `SegmentItem` | 一组互斥选项。滑块是**同一块玻璃挪过去** | 当主操作；自己再拼一套（尺寸迟早跑偏） |
| `ThemePicker` | 主题切换。**一片卡的网格**，每格带三点色卡，画的是**那套**主题的颜色 | 拿来当主操作 |
| `.glassPanel(_:radius:interactive:)` | 一块玻璃：卡片、行、浮层、容器 | 往它身上再加描边 / 渐变 / 投影（那三样是材质自己的） |
| `.readingSurface()` | 拿来**读和改**的地：代码框、结果区、长文本。稳定的不透明底色 | 当容器背景（那是 `.glassPanel()`） |
| `.field(focused:)` | 能打字的地方（同上 + 焦点环），聚焦时显示强调色实线 | 只读的结果框（那是 `.readingSurface()`） |
| `.softFill(_:)` | 上了语义色的一块玻璃：徽章、提示条 | 当主操作 |
| `.hairline(_:)` / `Hairline` | 一块玻璃**内部**的分区线 | 两块玻璃**之间**（它们自己就分得开） |
| `SelectionSurface` | 「你现在在这儿」。边栏 / 主题格 / 待办卡**共用它** | 拿它当装饰（它是全应用最重的 tint 之一） |
| `.smokedCard(accent:)` | **大卡**的底：材质 + 压沉的幕 + 细边 + 投影 | 往它身上加 `glassEffect`（那是第 4 档的事） |
| `GlassEffectContainer` | 把**挨着的**几块玻璃当一组渲染 | 包住不挨着的玻璃（只是让系统多算一遍） |
| `AmbientBackdrop` | 窗口级的氛围场 —— 玻璃的燃料 | 当装饰性背景往组件里塞 |

删掉的那些（`PageHeader`、`GlassChip`、`GlassFacets`、`SegmentTrack` / `SegmentItem`、
`.panel()`、`.bloom()`、`.glassBackground()`）都有同一个去处：**系统**。
对照表在上面「从「系统默认」到「设计过」，再走回来一半」。

`NoticeView` 画成有圆角有描边的**卡片**，不是通栏色带：色带是「系统横幅」的语汇，
读起来像应用在替别人传话。**不用左侧色条** —— 那是最偷懒的强调，
且色条本身不传达任何信息。

### 按钮

按钮是**交互玻璃**那一档（第 4 档）：小、短、会被按，光学反馈是交互的一部分。
所以它上真 `glassEffect`，而且是全应用敢上重 tint 的两处之一。

| | 材质 | 边 | 辉光 | 什么时候用 |
|---|---|---|---|---|
| `primary` | `.tint(accentFill 0.30)` + 底下一道同色渐变 | 同色 0.76 | 有 | 一屏最多一个 |
| `secondary` | `.regular` | 镜面边 | 无 | 常规操作 |
| `ghost` | `.clear` | **无** | 无 | 行内的弱操作 |
| `destructive` | `.regular` + 危险色**前景** | 危险色 0.42 | **无** | 删除、清空 |

**边是这四档之间最明显的差别，比底色明显。** 四种按钮的材质其实很接近，
真正让人一眼分出主次的是那圈边的颜色和浓度。

破坏性操作**只染前景和边，不染底**，而且不发光：一块红底的按钮在一行操作里
比它该有的分量重得多，辉光更是在**邀请人按它**。它要好认，不该好按。

`ghost` 走 `Glass.clear` 而不是「什么都不画」：`clear` 是系统给的更透的那一档
玻璃，仍然是同一种材质，只是几乎不占视觉重量 —— 比自己画一个「悬停才出底色」
的假按钮诚实，交互反馈也是系统的。

#### 这里走过一段弯路

中间有一版是**自己画主按钮的体**：一块 `accentFill` 实心填充、底缘压沉、
顶上一道 0.98 的亮带、外面两层辉光，另配一个近黑的 `onAccent` 当字色。

出来是**一颗塑料糖豆**。问题不在哪个参数调过了，在方向错了：

> 一块**不透明的亮色填充**永远不会读成玻璃，不管往上堆多少高光。
> 玻璃的颜色**来自透过它的光**，不是来自涂在它表面的漆。

现在的做法是把颜色交回给材质（`tint` + 底下一道同色渐变），
所以深色外观下主按钮是一块**深而透亮的青**，浅色外观下是一块**浅青** ——
它跟着氛围场走，因为它本来就在透着氛围场。

字色也跟着回到 `Ink.primary`（跟外观走），那个近黑的 `onAccent` 槽位随之删掉 ——
它存在的前提是「底是一块固定的亮色」，而这个前提没有了。

`ButtonStyle` 那条**没变**：只接管画法，点击 / ⏎ / Tab 焦点 / 辅助功能角色 /
`.disabled()` 传播一样不丢，材质和指针反馈仍然是系统的。

三档尺寸的高度和左右留白一起放大，字只跟半档 —— **按钮的体面来自留白，
不是字号**。三档都是胶囊，图标按钮因此是圆的（正方形配胶囊圆角就是个圆）。
**容器不跟着走**：卡片、面板里装的是成段的内容，胶囊会把第一行和最后一行的字
挤进弧线里。

---

---

## 状态表达

**形状 + 文字 + 颜色三重编码，任缺其一都还读得出来。**
色觉障碍、增强对比度、灰度截图都要能用。

```
等待   circle.dashed              中性徽章
进行中 ProgressView + 徽章         accent
成功   checkmark                  success + 输出文件名
失败   exclamationmark.triangle   danger + 整句原因（不折叠成错误码）
```

**颜色只在需要注意时出现。** 「就绪」这类常态是中性面板 + 一个绿点，
不是一整条绿色横幅 —— 常态不该抢注意力，把颜色留给出事的时候。

---

## 动效

150–250ms。只表达状态变化：分区切换的选中滑块、通道就绪 ↔ 不就绪的形态切换、
拖放高亮、按钮的悬停 / 按下。没有入场编排，没有滚动触发的揭示。

全部走 `Theme.Motion.x(reduceMotion:)`，减弱动态时返回 `nil`（直接切换）——
**SwiftUI 不会替你做这件事**。位移类效果（按下缩放）在减弱动态下整个去掉，
只留颜色变化。

### 选中态：试错了三轮

| 轮次 | 做法 | 结果 |
|---|---|---|
| 1 | 选中才创建，无动画 | **闪断** —— 旧的直接消失、新的直接出现 |
| 2 | 常驻一块玻璃，靠透明度进出 | **花眼** —— 淡入淡出的是材质本身 |
| 3 | 不上玻璃，只淡入淡出颜色 | 不刺眼了，但那块底也不像玻璃了 |
| **现在** | 上重 tint 的真玻璃，**不做动画** | ✓ |

第二轮的教训值得单独记一条：

> **一块玻璃「渐渐变得不折射」这件事现实里不存在**，所以眼睛读不懂它，
> 只觉得那一片在抖 —— 边栏那种场合还是十一行同时在抖。

第三轮修掉了刺眼，但为此把材质也一起丢了 —— 那块底退化成一层平色。

**现在的答案是：材质给足，动画不给。** 选中块是全应用最重的一处 tint
（真 `glassEffect` + 底下一道同色渐变 + 一圈 0.76 的同色边），
但它**不淡入淡出** —— 切换是瞬时的。

这不是妥协，这是 macOS 的原生行为：系统边栏的选中态从来不做过渡。
第一轮之所以读成「闪断」，是因为那时候**整屏只有那一块在变**；
现在切分区时标题栏、内容区、状态栏一起换，选中块瞬时跟上反而是对的 ——
它是那批变化里的一个，不是孤零零闪一下的那个。

**边栏、主题格、选中的待办卡是同一个组件**（`SelectionSurface`）。
三处说的都是「你现在在这儿」，长得不一样的话用户得学两遍。

上一版这里是 `matchedGeometryEffect` 的滑块位移。那套在自绘导航项上成立，
换成原生 `List` 之后不成立了：行背景各自独立，共享 namespace 跨不过去。

### 背景保持静止

能量星球、星光粒子和波纹都不漂移、不闪烁、不旋转，也不跟着页面滚动；
它们只在窗口或分栏尺寸变化时重新排布。
这样工具输入时没有持续抢视线的运动，也没有一层永不停的重绘。
减弱透明度会移除整颗星球，增强对比度会让 aura 与网格槽位归零。

---

## 八个踩过的 SwiftUI 坑

写新界面前先读，能省几个小时。前三条是同一类：**编译通过、看不出错、就是不工作**。

**1. 详情区里的多行文字不要加 `.fixedSize(horizontal: false, vertical: true)`。**

那是「让多行文字别被截断」的常见写法，但在 `NavigationSplitView` 的详情区里
它会让 SwiftUI 拿一个还没定下来的宽度去算高度，一段话能把自己撑到一千多点高，
**整个窗口的内容上下溢出** —— 侧边栏、状态条、底栏统统被顶出可视区，
界面看着像「没画出来」，而 AX 树里它们全在（带着屏幕外的坐标）。
`Text` 本来就会换行，父级给了宽度就够。同理**不要在文本列后面放 `Spacer`**。

**2. 放进 `focusedSceneValue` 的类型必须是 `Equatable`。**

body 每求值一次就造一个新值；判断不出「还是同一个」时 SwiftUI 会认为
焦点值每帧都在变，`invalidateProperties` → `setNeedsUpdate` → 约束更新 →
再求值，最后 AppKit 抛「Update Constraints in Window passes 超过视图数」，
整个应用崩掉。崩之前控制台会先留一句
`[Invalid Configuration] FocusedValue update tried to update multiple times per frame.`

**3. 同一个视图上不要挂两个 `.fileImporter`。**

只有最后一个生效，前面的按下去毫无反应 —— 编译通过、按钮在辅助功能树里、
点击事件也到了，面板就是不出来，控制台一个字都不打。
macOS 上直接用 `NSOpenPanel`（它本来就是 `.fileImporter` 背后的东西），
有窗口时 `beginSheetModal(for:)` 贴成 sheet，观感一致。

**4. `NavigationSplitView` 会偷偷给侧边栏那一列垫一块透到桌面的材质。**

那是系统的 `.sidebar` 材质（`behindWindow` 混合）。半透明的东西压在它上面，
桌面壁纸就从底下透上来了 —— 界面看着更「毛玻璃」，代价是那一列的对比度
从此算不出来（壁纸是用户的）。所以侧边栏**自己画一份不透明的氛围底**盖住它。

同一份氛围底在侧边栏和内容区各画一次。具名坐标空间跨不过分栏内部的
AppKit hosting 边界，所以根部把 global `CGRect` 通过 Environment 下发，
各区域再用自己的 `frame(in: .global)` 算相对偏移并裁切。
只下发窗口尺寸不够：窗口 global 原点不保证是 `(0, 0)`，必须传完整矩形。
这样两份才是同一张背景的不同裁片，不会在侧栏再长出一颗小星球。

**5. 输入框会偷偷改你打进去的字符。**

macOS 的 `NSTextView` / `NSTextField`（`TextEditor` 和 `TextField` 底下就是它们）
默认开着**智能引号替换**：你打一个 `"`，它给你一个 `“` 或 `”`。

在写文章的应用里这是贴心。在一个装着 JSON、JWT、PEM、正则的工具箱里这是灾难 ——
**在输入框里打出来的 JSON 自己解析不了**，而且 `"` 和 `“` 在大多数字体里
几乎看不出区别，报错只会说「第 1 行第 2 列有问题」。破折号替换（`--` → `—`）、
文本替换、自动纠错同理。

SwiftUI 没有关掉它的修饰符，得在**任何文本控件建出来之前**往本应用自己的域里写
`NSAutomaticQuoteSubstitutionEnabled` 那一组键（见 `AppDelegate`
的 `applicationWillFinishLaunching`）。应用域优先级高于系统全局设置，
也不影响别的应用。

解析那一侧也得收着：从聊天、文档、PPT 里粘进来的照样带中文引号，
关掉输入框的替换救不了它们。`JSONFormatter` 的做法是「试着修，修好了就在界面上
逐条说出来改了什么」—— **悄悄帮你修好是最难查的一类行为**。

**6. AppKit 的状态恢复会往 `List(selection:)` 里塞值。**

换成原生边栏列表之后冒出来的：连开三次应用，落在**三个不同的分区**上，
而 `AppState.selection` 的初始值明明写着「概览」。

不是竞态，是 AppKit 的 Saved Application State：它把列表的**选中行号**存下来，
下次启动塞回绑定里，于是 SwiftUI 反手把那个值写进了 `AppState`。
行号存下来还会对不上 —— 分组一变，第 7 行就不是同一个分区了。
删掉 `~/Library/Saved Application State/<bundle-id>.savedState` 之后三次全落在概览，
一试就准。

「当前在哪个分区」只能有一个出处。所以窗口挂 `.restorationBehavior(.disabled)`，
把系统那条旁路关掉。真要做「记住上次的分区」得**自己存**，
而不是让 AppKit 从一个行号里猜。

**7. 边栏的选中色 `.tint()` 管不着，但 `.listRowBackground()` 能整块换掉。**

`.tint()` 沿环境往下传，滑块、勾选框、聚焦环、`.glassProminent` 全认它 ——
**唯独边栏的选中高亮不认**。那块走的是 `NSColor.controlAccentColor`，
来自「系统设置 → 外观 → 强调色」，只有 bundle 里放一份资源目录的 `AccentColor`
才盖得掉。而本机只有 CLT，`actool` 要 Xcode 才跑得起来
（`/usr/bin/actool` 只是个转发器，跑起来就报错）。

出路是 `.listRowBackground()`：它把系统那块高亮**整块替换**掉，不是盖在上面 ——
所以自绘的玻璃底下不会渗出一圈蓝。结构和键盘行为仍然是系统列表的，
只有那块底是自己的。一块固定的蓝会让「换主题换掉整个界面」这句话当场破功，
这是六套色板里唯一必须自己画的一处。

**8. `@FocusState` 驱动不了 macOS 上的焦点环。**

`.focused($state, equals:)` 挂在 `Button` 上，改 `state` 的值**焦点环不会跟着动** ——
那圈环是 AppKit 的第一响应者画的，Tab 走得动，代码推不动。
所以别写「把焦点同步到选中项」这种逻辑，它不会生效。
启动时那圈多余的焦点环在 `AppDelegate.clearInitialFocusRing()` 里清掉。

---

## Accessibility

五项全要：减弱动态、**减弱透明度**、增强对比度、完整键盘操作、VoiceOver。

- **减弱透明度**一开，系统那层材质自己会让位 —— 这一版不再自己备一套实心色阶。
  还归我们管的是氛围场：减弱透明度时整颗星球不创建，增强对比度下光晕与网格
  alpha 归零（有测试盯着）。气氛不该挡路。

- **还给系统的那些控件，无障碍是白拿的**：边栏列表的上下键遍历、首字母跳转、
  `.isSelected` 语义，分段控件的单选组语义，按钮的角色和四态 —— 上一版是一件件
  手补的，这一版一行没写。
- **还剩几处自绘**（导航卡、待办卡、主题格），它们仍然要一件件补：
  `.accessibilityHint`、图标按钮的显式名称、
  `.contentShape(.focusEffect,)` 让系统焦点环跟着圆角走。
- **选中块（主题强调色）和焦点环（系统蓝）是两件事**，会同时出现在不同的项上：
  前者是「当前在哪个分区」，后者是「键盘现在停在哪」。开了「全键盘控制」时
  这是 macOS 的标准行为，不是 bug。
- 不要用 `.accessibilityElement(children: .combine)` 把一行合成一个元素 ——
  它会把里面的按钮吞掉，还会把一行状态文字误报成按钮。
- 输入框的 placeholder 不算 label：用户一输入内容占位就没了，
  控件随即变成无名的「文本栏」。显式给 `.accessibilityLabel`。
- 图标只做视觉重复时标 `.accessibilityHidden(true)`，别让 VoiceOver 念两遍。

### 控件点击区域与待办选择

自绘按钮和分段选项的点击区域至少为 44×44 点，玻璃本体保留原有视觉尺寸。
待办卡片提供独立的选择、完成和删除按钮，不嵌套按钮；选择支持键盘激活和选中语义。
删除按钮在悬停、选中或卡片内部具有键盘焦点时显示，位置始终保留。
